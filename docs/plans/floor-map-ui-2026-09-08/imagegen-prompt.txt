Use case: ui-mockup.
Create a high-fidelity design proposal board for the floor-map section of this warehouse storage planner, using the most recent attached screenshot as reference for the existing dark theme, data and visual problem. This is a proposed UI, not an implemented screenshot.
Output wide landscape 1800x1300, crisp readable UI.
Show a large redesigned 3D floor workspace across the upper two-thirds. Header "Floor 4" and "10 × 10 × 3 m". Compact search "Find location or pallet…" and outlined icon-only view controls for 2D / 3D, zoom, fit, rotate. Keep all controls minimal, no filled backgrounds except selected view.
The 10x10 floor should be a thin flat isometric slab, NOT a giant solid box. The floor height is a clearance dimension, not thick floor concrete. Fit floor to view with room for callouts. Faint grid, thin floor perimeter, no competing ghost floor by default.
Precisely show TWO 2x2 storage locations on the floor: FG-1, code Z01, height limit3m, contains ONE small tan pallet P-000003 of dimensions1.2x1x1.4m; and "Storage 2", Z02, limit1m, empty. Zone boundary is teal outline and subtle floor tint, NOT a solid green cube. Show real pallet volume separately as tan/brown block. Only selected FG-1 has faint dashed wireframe height limit, blue selection boundary and handles in edit mode only. Legible floating labels "FG-1 · 1 pallet" and "Storage 2 · Empty" outside geometry with leader lines, no overlaps. Do not invent extra live locations.
On the right, a slim selected-location detail panel: "FG-1", "BLDG-A-F04-Z01", dimensions 2×2×3m, stored P-000003, coordinates X0.2 Y0.3 Z0m. Accessible-looking outline icon-only actions pencil, eye, move, QR. No raw QR payload. Small hint "Select to inspect · Edit to move".
Bottom third: labeled design-spec strip with FIVE enlarged separate miniature location-state specimens: Empty (outline zone only), Stored (tan pallet inside zone), Reserved (amber dashed pallet footprint), Selected (blue perimeter and detail focus), Invalid edit (red outline + warning icon + short reason "Outside floor"). These are visual state examples, not extra floor locations. Include a small 2D inset illustrating top-down outlines and pallet footprints matching FG-1 and Storage2. Labels use plain English for legibility, with Thai main section title "พื้นที่ชั้น". Professional restrained dark slate interface, neutral text, blue selection, teal location outline, tan physical pallets, amber reservations, red invalid. Plenty of contrast. No gradients, no glossy marketing art, no huge QR codes, no needless statistic cards. Main priority: immediately distinguish available location footprint, permitted height, and actual stored pallet.
