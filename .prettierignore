# Generated / vendored output — never formatted.
node_modules/
.next/
.next-preview/
# The dedicated Playwright build directories (see playwright.config.ts).
.next-e2e/
.next-e2e-preview/
out/
build/
dist/
coverage/
test-results/
playwright-report/
blob-report/
convex/_generated/
graphify-out/
next-env.d.ts
pnpm-lock.yaml

# Generated by `pnpm manual:generate`. The bytes are produced from
# `scripts/manual/tasks.mjs`, and `pnpm manual:check` compares them against a
# fresh render — reformatting them here would make that comparison fail.
docs/manual-html/

# Approved planning document: must stay byte-for-byte identical.
PROJECT_PLAN.md
